#pragma once
#include <Graphics.hpp>
const int HEIGHT_MAP = 25;//размер карты высота
const int WIDTH_MAP = 40;//размер карты ширина 

using namespace sf;

class mapa : public essence
{
protected:
	sf::String TileMap[HEIGHT_MAP];	
public:
	mapa(String F);

	print(RenderWindow& window);
};
